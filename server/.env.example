# Copie para .env e preencha

# Connection string do Supabase (dashboard -> Connect -> Session pooler, porta 5432)
DATABASE_URL=postgresql://postgres.SEU_REF:SUA_SENHA@aws-0-REGIAO.pooler.supabase.com:5432/postgres

# Segredo do JWT — gere com: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET=

# PORT=3001
